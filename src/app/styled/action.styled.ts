import styled from 'styled-components';

export const ActionFloating = styled.div`
  position: fixed;
  bottom: 0;
  background: var(--body-bg);
  width: 100%;
  padding: 1rem 0;
  /* border-top: 1px solid var(--black-50); */
`;
